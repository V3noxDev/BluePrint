#!/bin/bash
# Runs automatically when the extension is removed via `blueprint -remove`.

echo ""
echo "  Properties Editor has been removed."
echo "  The 'Properties' tab and file-manager button are gone; your"
echo "  server.properties files were never touched by the uninstall."
echo ""

exit 0
